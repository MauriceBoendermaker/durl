import {sdk} from './CirclesConfig'

const CONTRACT_ADDRESS = process.env.REACT_APP_CONTRACT_ADDRESS as `0x${string}`;

// const avatar = await sdk.getAvatar(CONTRACT_ADDRESS);
// console.log("avatar info: " + avatar.avatarInfo);
// export default avatar;
